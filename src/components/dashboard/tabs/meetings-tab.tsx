import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function MeetingsTab() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Meetings Schedule</CardTitle>
        <CardDescription>This section is being migrated to Firebase.</CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Meeting management will be available here shortly.</p>
      </CardContent>
    </Card>
  );
}
